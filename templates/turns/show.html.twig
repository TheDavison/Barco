{% extends 'base.html.twig' %}

{% block title %}Turns{% endblock %}

{% block body %}
    <h1>Turns</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ turn.id }}</td>
            </tr>
            <tr>
                <th>Hour</th>
                <td>{{ turn.hour }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_turns_index') }}">back to list</a>

    <a href="{{ path('app_turns_edit', {'id': turn.id}) }}">edit</a>

    {{ include('turns/_delete_form.html.twig') }}
{% endblock %}
